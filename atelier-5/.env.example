# Copier ce fichier en .env pour changer la config en local
# Tag de l'image à lancer avec docker-compose.deploy.yml (local = image construite sur ma machine)
IMAGE_TAG=local
# Identifiants de Grafana (http://localhost:3000)
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=change-moi
