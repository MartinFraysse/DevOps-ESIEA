# Liste blanche : on exclut tout, puis on ne réautorise que ce qui sert au build.
# Évite d'embarquer .git, .env, .venv, caches, tests ou captures d'écran dans l'image.
*
!app.py
!requirements.txt
# Utilisé uniquement par Dockerfile.naive (image naïve de comparaison)
!requirements-dev.txt
